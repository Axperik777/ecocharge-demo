const content = require('./trust-content.json');

// Assets and destinations are explicitly supplied by the project team, never inferred.
function safeUrl(value) {
  if (!value) return '';
  if (/^assets\/[a-zA-Z0-9_./-]+$/.test(value) && !value.includes('..')) return value;
  try { const u = new URL(value); return u.protocol === 'https:' ? u.href : ''; } catch { return ''; }
}

module.exports = function ({esc, icon, kicker, link, button, faq}) {
  const pending = label => `<span class="ec-evidence-status">${label || 'Reference guide'}</span>`;
  const date = value => value ? esc(value) : 'Not supplied';
  const field = value => value ? esc(value) : '<span class="ec-pending-value">To be provided</span>';

  const rights = `<section class="ec-section ec-wrap ec-rights" id="participation"><div class="ec-section-heading"><div>${kicker('BEFORE YOU BUILD A PLAN')}<h2>Know what your<br>participation means.</h2></div><p>Start with three questions. These answers belong in the final documents, before any real funding.</p></div><div class="ec-rights-grid">
    <article>${icon('file')}<h3>What would I own?</h3><p>The proposed concept is participation in a charging business. The exact instrument and client rights are still to be defined.</p>${pending('Participation terms pending')}${link('Review client rights','terms/#client-rights')}</article>
    <article>${icon('grid')}<h3>Who is responsible?</h3><p>EcoCharge is the project brand. The registered issuer and the entity signing an agreement must be identified.</p>${pending('Company details pending')}${link('Company & people','about/#company')}</article>
    <article>${icon('bolt')}<h3>What funds a payout?</h3><p>Charging payments are the proposed revenue source. Costs, reserves and distribution rules determine what can be paid.</p>${pending('Operating evidence pending')}${link('Follow the money','how-it-works/#cash-flow')}</article>
  </div><p class="ec-section-footnote">A demo account does not create shares, ownership or a right to receive payments.</p></section>`;

  function video() {
    const src = safeUrl(content.video.src), poster = safeUrl(content.video.poster), captions = safeUrl(content.video.captions);
    const media = src ? `<video class="ec-intro-video" controls playsinline preload="none"${poster ? ` poster="${esc(poster)}"` : ''} aria-label="EcoCharge company introduction"><source src="${esc(src)}" type="video/mp4">${captions ? `<track default kind="captions" srclang="en" label="English" src="${esc(captions)}">` : ''}Your browser cannot play this video.</video>` : `<div class="ec-video-placeholder"><span class="ec-video-label">COMPANY INTRODUCTION</span><div class="ec-video-diagram" aria-hidden="true"><span>${icon('bolt')}</span><i></i><span>${icon('grid')}</span><i></i><span>${icon('file')}</span></div><strong>Meet the people.<br>Understand the business.</strong><span class="ec-video-pending">Video coming soon</span><p>This space is reserved for an introduction<br>from the actual project team.</p></div>`;
    return `<section class="ec-section ec-wrap ec-introduction" id="introduction">${media}<div class="ec-introduction-copy">${kicker('A CLEAR INTRODUCTION')}<h2>Get the full picture.<br>At your own pace.</h2><p>Learn what the project proposes, how the account works and which questions still need documented answers.</p><ul class="ec-intro-chapters"><li><span>The business</span>How charging activity could support participation</li><li><span>Your account</span>Stations, calculations, documents and conversations</li><li><span>The next step</span>What needs to be verified before real funding</li></ul><button class="ec-button ec-button-outline" data-trust="intro">Read the introduction ${icon('arrow')}</button></div></section>`;
  }

  function cashFlow(full = false) {
    const steps = [['Charging revenue','Drivers pay for electricity and charging service.'],['Operating costs','Electricity, site costs, service and reserves.'],['Business result','The amount remaining after the example costs.'],['Distribution rules','Company and client rights determine any payout.']];
    return `<section class="ec-section ec-cashflow${full?'':' ec-wrap'}" id="cash-flow"><div class="ec-section-heading"><div>${kicker('FOLLOW THE MONEY')}<h2>From a charging session<br>to a possible distribution.</h2></div><p>Revenue is the starting point. The business must cover its costs before a distribution can be considered.</p></div><ol class="ec-flow-steps">${steps.map(([title,copy],i)=>`<li><span class="ec-flow-number">0${i+1}</span><h3>${title}</h3><p>${copy}</p></li>`).join('')}</ol>${full ? `<div class="ec-operating-example"><div class="ec-operating-controls"><span class="ec-badge">TEACHING EXAMPLE · ONE WEEK</span><h3>What if charging activity changes?</h3><p>Invented round numbers to explain costs. These are not station results, forecasts or the basis of a plan rate.</p><div class="ec-scenario-buttons" role="group" aria-label="Choose a charging activity example"><button data-cash-scenario="example" aria-pressed="true">Example week</button><button data-cash-scenario="lower" aria-pressed="false">Lower activity</button><button data-cash-scenario="offline" aria-pressed="false">No charging</button></div><p class="ec-operating-assumption">In this illustration, electricity changes with receipts. Site, service and reserve amounts stay fixed. Real costs may behave differently.</p></div><div class="ec-operating-ledger"><dl><div><dt>Charging revenue</dt><dd id="cash-revenue">$1,000.00</dd></div><div><dt>Electricity</dt><dd id="cash-electricity">−$350.00</dd></div><div><dt>Site costs</dt><dd>−$150.00</dd></div><div><dt>Service & operations</dt><dd>−$120.00</dd></div><div><dt>Reserve allowance</dt><dd>−$80.00</dd></div><div class="ec-cash-result"><dt>Balance after example costs<small>Before tax and distribution rules</small></dt><dd id="cash-result">$300.00</dd></div></dl><p id="cash-explanation" role="status">The $300 balance is not a client payout. Company and client distribution rules are not defined.</p></div></div><p class="ec-section-footnote">The 43% assumption is not applied here: its scope and entitlement to profits are unverified. A fixed weekly rate cannot be inferred from this example.</p>` : `<div class="ec-flow-footer"><p>Explore a lower-activity week and a week with no charging.</p>${link('Explore the cost example','how-it-works/#cash-flow')}</div>`}</section>`;
  }

  const termsItems = [
    ['client-rights','Your legal rights','The instrument, contracting entity and rights attached to your participation.','Participation agreement pending'],
    ['capital-access','Access to capital','When capital may be returned, notice requirements and any restrictions.','Exit terms pending'],
    ['lock-period','Holding period','Any minimum term, lock-up or limits on transferring your interest.','Holding terms pending'],
    ['fees','Fees and deductions','Platform charges, operating deductions, withdrawal fees and applicable taxes.','Fee schedule pending'],
    ['funds','Who handles the money','The legal recipient, account holder and any custody or payment arrangements.','Payment arrangements pending'],
    ['risks','Losses and delays','What happens during downtime, a revenue shortfall or a failure to make distributions.','Risk document pending']
  ];
  function terms(full = false) {
    return `<section class="ec-section ec-terms-section" id="terms-at-a-glance"><div class="ec-section-heading"><div>${kicker('TERMS AT A GLANCE')}<h2>Understand access.<br>Know the conditions.</h2></div><p>These terms are not finalized. The demo accepts no money and establishes no withdrawal or investment rights.</p></div><div class="ec-terms-grid">${termsItems.map(([id,title,copy,status])=>`<article${full?` id="${id}"`:''}><h3>${title}</h3><p>${copy}</p>${pending(status)}</article>`).join('')}</div>${full?'':`<div class="ec-flow-footer"><p>Review the open questions before choosing a real investment.</p>${link('Read access, fees & risks','terms/')}</div>`}</section>`;
  }

  function documents() {
    return `<section class="ec-section ec-evidence" id="company-documents"><div class="ec-section-heading"><div>${kicker('COMPANY & DOCUMENTS')}<h2>Evidence you should<br>be able to inspect.</h2></div><p>Each item has its own status. A sample or an uploaded file is not independent verification.</p></div><div class="ec-evidence-top"><span>Document register</span><span>Page updated <time datetime="${esc(content.updatedAt)}">${esc(content.updatedAt)}</time></span></div><div class="ec-evidence-list">${content.documents.map(d=>{const href=safeUrl(d.href);return `<article><div>${icon('file')}<div><h3>${esc(d.title)}</h3><p>${esc(d.description)}</p></div></div><div class="ec-evidence-meta">${pending(href?'Provided · review required':'Awaiting document')}${d.publishedAt?`<small>Document date: ${date(d.publishedAt)}</small>`:''}</div>${href?`<a class="ec-text-link" href="${esc(href)}" target="_blank" rel="noopener">Open document ↗</a>`:`<button class="ec-text-button" data-trust-document="${esc(d.id)}">What this should cover ${icon('arrow')}</button>`}</article>`;}).join('')}</div><p class="ec-section-footnote">Pending documents have no download file. The sample participation summary above is a demonstration, not an executed agreement.</p></section>`;
  }

  function company() {
    if (!content.company.legalName) return `<section class="ec-section ec-company-identity" id="company"><div>${kicker('ABOUT THIS PROJECT')}<h2>One product.<br>Two connected workspaces.</h2><p>ECO CHARGE is an academic project about an EV-charging investment platform. It brings the client experience and account management into one working interface.</p>${link('Explore the account','client/?guest=1&tab=dashboard')}</div><dl><div><dt>Client workspace</dt><dd>Station research, plan calculations, messages and documents.</dd></div><div><dt>Team workspace</dt><dd>Account settings, funding requests and weekly records.</dd></div><div><dt>Project data</dt><dd>Public station records and simulated financial activity. No real payments or investment rights.</dd></div></dl></section>`;
    const labels = [['legalName','Registered legal name'],['jurisdiction','Jurisdiction'],['registrationNumber','Registration number'],['registeredAddress','Registered address'],['instrument','Proposed client instrument'],['networkScope','Network covered by the 43% assumption']];
    return `<section class="ec-section ec-company-identity" id="company"><div>${kicker('THE ENTITY BEHIND THE PROJECT')}<h2>A brand and a legal entity<br>have different roles.</h2><p>ECO CHARGE identifies this demo. The company that would issue an investment and sign the agreement still needs to be documented.</p>${link('View the document register','resources/#company-documents')}</div><dl>${labels.map(([key,label])=>`<div><dt>${label}</dt><dd>${field(content.company[key])}</dd></div>`).join('')}</dl></section>`;
  }

  function team() {
    const t=content.team,photo=safeUrl(t.photo),booking=safeUrl(t.bookingUrl);
    if (!t.name) return `<section class="ec-section ec-team-section" id="team"><div>${kicker('STAY IN THE CONVERSATION')}<h2>Your questions.<br>One conversation.</h2><p>Open a conversation from your plan, ask about a station or discuss the calculation with your account manager.</p>${link('Open my inbox','client/?guest=1&tab=dashboard&contact=manager')}</div><article class="ec-team-profile"><span class="ec-card-label">HOW MESSAGES WORK</span><h3>Client and team, connected.</h3><p>A question sent from the client account appears in the team workspace. The reply returns to the same conversation.</p><ol class="ec-intro-chapters"><li><span>Ask</span>Send a question from your account.</li><li><span>Reply</span>Open the team workspace and respond.</li><li><span>Follow up</span>Review the reply in the client inbox.</li></ol><small class="ec-team-note">Conversations are stored in this browser. No external messages or calls are sent.</small></article></section>`;
    const channels=[['Email',t.email&&/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(t.email)?'mailto:'+t.email:''],['WhatsApp',/^\+?[1-9]\d{7,14}$/.test(t.phone)?'https://wa.me/'+t.phone.replace('+',''):''],['Telegram',/^[a-zA-Z][\w]{4,31}$/.test(t.telegram)?'https://t.me/'+t.telegram:'']];
    return `<section class="ec-section ec-team-section" id="team"><div>${kicker('PEOPLE & CONTACT')}<h2>Put a person<br>behind the conversation.</h2><p>The real team profile, working hours and contact channels will appear here when supplied.</p>${link('Try the demo conversation','register/?next=dashboard')}</div><article class="ec-team-profile"><div class="ec-team-person">${photo?`<img src="${esc(photo)}" alt="${esc(t.name||'Team member')}" width="76" height="76" loading="lazy">`:'<div class="ec-team-avatar" aria-label="Team photo placeholder"><svg viewBox="0 0 64 64" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true"><circle cx="32" cy="23" r="10"/><path d="M13 53v-4a19 19 0 0 1 38 0v4"/></svg></div>'}<div><span class="ec-card-label">PROJECT TEAM</span><h3>${esc(t.name||'Team profile coming soon')}</h3><p>${esc(t.role||'Name, role and photo to be provided')}</p></div></div><dl><div><dt>Working hours</dt><dd>${field(t.hours)}</dd></div><div><dt>Time zone</dt><dd>${field(t.timeZone)}</dd></div></dl><div class="ec-team-channels">${channels.map(([label,href])=>href?`<a href="${esc(href)}"${href.startsWith('https:')?' target="_blank" rel="noopener"':''}>${label} ↗</a>`:`<button data-trust="contact">${label}<small>Not connected</small></button>`).join('')}</div>${booking?`<a class="ec-button ec-button-outline" href="${esc(booking)}" target="_blank" rel="noopener">Schedule a conversation ↗</a>`:`<button class="ec-button ec-button-outline" data-trust="contact">Video meeting · coming soon ${icon('arrow')}</button>`}<small class="ec-team-note">The demo inbox is local to your browser. It does not contact a live support team.</small></article></section>`;
  }

  const questions = `<section class="ec-section ec-wrap ec-trust-questions"><div>${kicker('BEFORE YOU GO FURTHER')}<h2>Questions worth asking.</h2>${link('See all documents & answers','resources/#company-documents')}</div><div>${faq('Do the stations shown belong to EcoCharge?','Ownership and affiliation have not been established. The catalog uses public location records. The future ownership documents must connect the company to specific assets.')}${faq('What happens if a station earns less?','Operating costs continue and funds available for distribution may decrease or disappear. The actual allocation of losses and any payment obligations must be specified in the final terms.')}${faq('Can I take my money out at any time?','Real withdrawal conditions have not been supplied. The demo has no actual deposits, lock-up periods or transferable investment rights. Read the access and exit terms before considering real funding.')}</div></section>`;

  const termsPage = `<div class="ec-wrap"><div class="ec-page-heading">${kicker('ACCESS, FEES & RISKS')}<h1>Read the conditions.<br>Before making a commitment.</h1><p>Understand the questions an agreement must answer. All financial activity in this product is a demonstration.</p></div>${terms(true)}<section class="ec-section ec-two-col"><div><h2>When activity changes,<br>the economics change.</h2><p>Equipment downtime, lower demand and operating costs can reduce available funds. Investment capital may be at risk; the demo establishes no capital protection.</p>${link('Explore the cost example','how-it-works/#cash-flow')}</div><div>${faq('Does a demo withdrawal prove I can withdraw real money?','No. It is a local interface simulation. It does not prove liquidity, custody arrangements, bank processing or a contractual right to withdraw.')}${faq('Are weekly rates supported by operating results?','No operating evidence is connected. The weekly rates shown inside the account are demo assumptions, not verified or guaranteed returns. More selected stations alone does not justify a higher return.')}${faq('Where will the finalized conditions appear?','The company and document register lists participation rights, fees, withdrawal terms and risk disclosures separately. Each file remains pending until supplied.')}</div></section><section class="ec-example-band"><div><h2>Keep the terms within reach.</h2><p>Review the document register and try the product with sample details.</p></div>${button('Company & documents','resources/#company-documents')}</section></div>`;
  return {rights,video,cashFlow,terms,documents,company,team,questions,termsPage};
};
